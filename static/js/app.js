const API='';
let currentCategory=null;
let currentArticle=null;
document.addEventListener('DOMContentLoaded',()=>{loadStats();loadFeatured();loadArticles();initScrollReveal();});
async function loadStats(){try{const r=await fetch('/api/stats');const d=await r.json();animateNum(document.getElementById('statArticles'),d.articles);animateNum(document.getElementById('statViews'),d.views);animateNum(document.getElementById('statComments'),d.comments);}catch(e){}}
function animateNum(el,target){if(!el)return;let start=0;const dur=1200;const t0=performance.now();function step(now){const p=Math.min((now-t0)/dur,1);const e=1-Math.pow(1-p,3);el.textContent=Math.round(start+(target-start)*e);if(p<1)requestAnimationFrame(step);}requestAnimationFrame(step);}
async function loadFeatured(){try{const r=await fetch('/api/articles?limit=3');const arts=await r.json();const c=document.getElementById('featuredCards');c.innerHTML=arts.slice(0,3).map((a,i)=>`<div class="featured-card scroll-reveal" style="animation-delay:${i*0.15}s" onclick="openArticle('${a.slug}')"><img src="${a.image_url}" alt="" loading="lazy"><div class="featured-card-overlay"><div class="fc-category">${a.category}</div><div class="fc-title">${esc(a.title)}</div><div class="fc-subtitle">${esc(a.subtitle||'')}</div></div></div>`).join('');initScrollReveal();}catch(e){}}
async function loadArticles(category){const grid=document.getElementById('articlesGrid');grid.innerHTML='<div class="loading-state"><div class="loader-ring"></div><span>LOADING INTEL...</span></div>';try{const url=category?`/api/articles?category=${category}`:'/api/articles';const r=await fetch(url);const arts=await r.json();document.getElementById('articleCount').textContent=`- ${arts.length} PIECE${arts.length!==1?'S':''}`;if(!arts.length){grid.innerHTML='<div class="loading-state"><span>NO INTEL FOUND</span></div>';return;}grid.innerHTML=arts.map((a,i)=>`<div class="article-card scroll-reveal" style="animation-delay:${(i%6)*0.08}s" onclick="openArticle('${a.slug}')"><div class="card-image"><img src="${a.image_url}" alt="" loading="lazy"><span class="card-category-badge badge-${a.category}">${a.category.toUpperCase()}</span></div><div class="card-body"><div class="card-title">${esc(a.title)}</div><div class="card-subtitle">${esc(a.subtitle||'')}</div><div class="card-meta"><span class="card-views">${a.views} VIEWS</span><span class="card-read-more">READ -&gt;</span></div></div></div>`).join('');initScrollReveal();}catch(e){grid.innerHTML='<div class="loading-state"><span>ERROR LOADING</span></div>';}}
function filterCategory(cat){currentCategory=cat;document.querySelectorAll('.cat-pill').forEach(p=>p.classList.remove('active'));document.querySelectorAll('.nav-link').forEach(l=>l.classList.remove('active'));if(!cat){document.querySelector('.cat-pill:first-child')?.classList.add('active');document.querySelector('.nav-link[data-page="home"]')?.classList.add('active');document.getElementById('sectionTitle').textContent='ALL INTEL';}else{document.querySelectorAll('.cat-pill').forEach(p=>{if(p.textContent.toLowerCase()===cat.toLowerCase()||p.textContent.toUpperCase()===cat.toUpperCase())p.classList.add('active');});document.querySelector(`.nav-link[data-cat="${cat}"]`)?.classList.add('active');document.getElementById('sectionTitle').textContent=cat.toUpperCase();}loadArticles(cat);closeArticle(true);document.getElementById('articles-section').scrollIntoView({behavior:'smooth'});}
async function openArticle(slug){const det=document.getElementById('article-detail');const main=document.querySelectorAll('#featured-strip,#articles-section');const hero=document.getElementById('hero-section');try{const r=await fetch(`/api/articles/${slug}`);if(!r.ok)return;const art=await r.json();currentArticle=art;hero.style.display='none';main.forEach(s=>s.style.display='none');det.classList.remove('hidden');renderDetail(art);renderComments(art);window.scrollTo({top:0,behavior:'smooth'});}catch(e){}}
function renderDetail(art){const content=art.content;const bc=`badge-${art.category}`;let html='';let sr=false;content.forEach(b=>{if(b.type==='stat'){if(!sr){html+='<div class="stats-row">';sr=true;}html+=`<div class="content-stat"><div class="stat-value">${esc(b.value)}</div><div class="stat-label-small">${esc(b.label)}</div></div>`;}else{if(sr){html+='</div>';sr=false;}if(b.type==='intro')html+=`<p class="content-intro">${esc(b.text)}</p>`;else if(b.type==='heading')html+=`<h2 class="content-heading">${esc(b.text)}</h2>`;else if(b.type==='paragraph')html+=`<p class="content-paragraph">${esc(b.text)}</p>`;else if(b.type==='quote')html+=`<div class="content-quote"><blockquote>${esc(b.text)}</blockquote><cite>${esc(b.source||'')}</cite></div>`;else if(b.type==='spec_table')html+=`<table class="spec-table"><thead><tr><th>Config</th><th>CPU</th><th>GPU</th><th>RAM</th><th>Storage</th></tr></thead><tbody>${b.rows.map(r=>`<tr><td>${esc(r.tier)}</td><td>${esc(r.cpu)}</td><td>${esc(r.gpu)}</td><td>${esc(r.ram)}</td><td>${esc(r.storage)}</td></tr>`).join('')}</tbody></table>`;}});if(sr)html+='</div>';const tags=JSON.parse(art.tags||'[]');document.getElementById('articleContent').innerHTML=`<img class="detail-hero-img" src="${art.image_url}" alt="" loading="lazy"><span class="card-category-badge ${bc} detail-category-tag">${(art.category||'').toUpperCase()}</span><h1 class="detail-title">${esc(art.title)}</h1>${art.subtitle?`<p class="detail-subtitle">${esc(art.subtitle)}</p>`:''}<div class="detail-meta"><span>${art.views} VIEWS</span><span>${art.comments?.length||0} COMMENTS</span><span>${tags.map(t=>'#'+t).join(' ')}</span></div>${html}<div class="vote-bar"><span class="vote-label">WAS THIS USEFUL?</span><button class="vote-btn up" onclick="vote('up')"><span id="upvoteCount">${art.upvotes||0}</span> YEAH</button><button class="vote-btn down" onclick="vote('down')"><span id="downvoteCount">${art.downvotes||0}</span> NAH</button></div>`;}
function renderComments(art){const cs=art.comments||[];document.getElementById('commentsSection').innerHTML=`<h3 class="comments-title">COMMENTS (${cs.length})</h3><div class="comment-form"><input type="text" id="commentAuthor" placeholder="YOUR HANDLE (OPTIONAL)" maxlength="50"><textarea id="commentText" placeholder="DROP YOUR TAKE..." maxlength="1000"></textarea><button class="comment-submit" onclick="submitComment()">POST COMMENT</button></div><div id="commentsList">${cs.length===0?'<p class="no-comments">NO COMMENTS YET - BE FIRST</p>':cs.map(c=>renderComment(c)).join('')}</div>`;}
function renderComment(c){return `<div class="comment-item"><div class="comment-author">${esc(c.author)}<span class="comment-date">${fmtDate(c.created_at)}</span></div><div class="comment-text">${esc(c.content)}</div></div>`;}
async function submitComment(){if(!currentArticle)return;const author=document.getElementById('commentAuthor').value.trim()||'Anonymous';const content=document.getElementById('commentText').value.trim();if(!content)return;const btn=document.querySelector('.comment-submit');btn.textContent='POSTING...';btn.disabled=true;try{const r=await fetch(`/api/articles/${currentArticle.slug}/comment`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({author,content})});if(r.ok){document.getElementById('commentAuthor').value='';document.getElementById('commentText').value='';const list=document.getElementById('commentsList');const no=list.querySelector('.no-comments');if(no)no.remove();list.insertAdjacentHTML('afterbegin',renderComment({author,content,created_at:new Date().toISOString()}));const h3=document.querySelector('.comments-title');const cur=parseInt(h3.textContent.match(/\d+/)?.[0]||'0');h3.textContent=`COMMENTS (${cur+1})`;}}catch(e){}btn.textContent='POST COMMENT';btn.disabled=false;}
async function vote(type){if(!currentArticle)return;try{const r=await fetch(`/api/articles/${currentArticle.slug}/vote`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type})});const d=await r.json();document.getElementById('upvoteCount').textContent=d.upvotes;document.getElementById('downvoteCount').textContent=d.downvotes;}catch(e){}}
function closeArticle(silent=false){const det=document.getElementById('article-detail');const main=document.querySelectorAll('#featured-strip,#articles-section');const hero=document.getElementById('hero-section');det.classList.add('hidden');hero.style.display='';main.forEach(s=>s.style.display='');currentArticle=null;if(!silent)window.scrollTo({top:0,behavior:'smooth'});}
function showHome(){currentCategory=null;closeArticle(true);document.querySelectorAll('.cat-pill').forEach((p,i)=>{if(i===0)p.classList.add('active');else p.classList.remove('active');});document.querySelectorAll('.nav-link').forEach(l=>{if(l.dataset.page==='home')l.classList.add('active');else l.classList.remove('active');});document.getElementById('sectionTitle').textContent='ALL INTEL';loadArticles();window.scrollTo({top:0,behavior:'smooth'});}
function initScrollReveal(){const ob=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');ob.unobserve(e.target);}});},{threshold:0.1,rootMargin:'0px 0px -50px 0px'});document.querySelectorAll('.scroll-reveal').forEach(el=>{if(!el.classList.contains('visible'))ob.observe(el);});}
function esc(str){if(!str)return'';return String(str).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;');}
function fmtDate(s){if(!s)return'';try{const d=new Date(s);return d.toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'});}catch(e){return s;}}